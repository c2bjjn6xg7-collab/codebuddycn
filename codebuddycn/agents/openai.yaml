interface:
  display_name: "CodeBuddy CN SDK"
  short_description: "Call, switch, and resume CodeBuddy China accounts via SDK"
  default_prompt: "Use $codebuddycn to ask a CodeBuddy Chinese Site model through the official Agent SDK for an independent second opinion and return its answer."
policy:
  allow_implicit_invocation: true
